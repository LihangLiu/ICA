function [ X ] = norm_signals( X )
% X: (n,t)

    for i=1:size(X,1)
        X_sub = X(i,:);
        
    end
    X_sub = X(i,range);
    minx = min(X_sub);
    maxx = max(X_sub);
    X_norm = (X_sub-minx)/(maxx-minx);


end

